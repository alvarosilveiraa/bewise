import { Color } from "./Color";

export type ThemeColorsConfig = {
  accent?: Color;
  background?: Color;
  danger?: Color;
  default?: Color;
  divider?: Color;
  foreground?: Color;
  primary?: Color;
  secondary?: Color;
  success?: Color;
  warning?: Color;
};
